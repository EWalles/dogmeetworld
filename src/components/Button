function Button (props) {

    const handleSubmit = () => {
            props.handleSubmitFromApp()
    };

    return (
        <button type="button" onClick={handleSubmit}>New Doggie!</button>
    )
}

export default Button;